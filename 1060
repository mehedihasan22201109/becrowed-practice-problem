#include <stdio.h>

int main()
{
    double number, PCount = 0;

    for (int i = 0; i < 6; i++)
    {
        scanf("%lf", &number);

        if (number > 0)
        {
            PCount++;
        }
    }

    printf("%.lf valores positivos\n", PCount);

    return 0;
}
